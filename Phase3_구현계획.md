# Phase 3 — 운영/배포/정리 상세 구현 계획

## 📋 개요
- **목표**: MVP 코드를 운영 가능한 상태로 정리하고 배포 준비
- **예상 소요**: 0.5~1일
- **현재 상태**: Phase 1-2 완료, 코드 분리됨 (`app.py`, `data.py`, `charts.py`)

---

## 🎯 Phase 3 세부 작업 항목

### 3.1 코드 구조화 및 품질 개선 (2~3시간)

#### 3.1.1 코드 리뷰 및 리팩토링
- [ ] **각 모듈 역할 명확화**
  - `data.py`: 데이터 로딩, 전처리, 필터링 함수만 포함
  - `charts.py`: 차트 생성 함수만 포함 (Altair/Plotly 등)
  - `app.py`: Streamlit UI 구성 및 사용자 인터랙션만 포함

- [ ] **함수별 docstring 작성**
  ```python
  def load_congestion_data(filepath: str) -> pd.DataFrame:
      """
      지하철 혼잡도 CSV 파일을 로드하고 전처리합니다.
      
      Args:
          filepath: CSV 파일 경로
          
      Returns:
          전처리된 DataFrame (long format)
          
      Raises:
          FileNotFoundError: 파일이 존재하지 않을 때
          UnicodeDecodeError: 인코딩 오류 발생 시
      """
  ```

- [ ] **타입 힌트 추가**
  - 모든 함수에 입력/출력 타입 명시
  - `from typing import List, Dict, Optional, Tuple` 활용

- [ ] **매직 넘버 제거**
  - 상수를 파일 상단에 정의
  ```python
  # constants
  DEFAULT_TOP_N = 20
  DEFAULT_ENCODING = 'cp949'
  TIME_COLUMNS_START = '05:30'
  TIME_COLUMNS_END = '00:30'
  ```

#### 3.1.2 에러 핸들링 강화
- [ ] **파일 로딩 예외 처리**
  ```python
  try:
      df = pd.read_csv(filepath, encoding='cp949')
  except FileNotFoundError:
      st.error(f"파일을 찾을 수 없습니다: {filepath}")
      st.stop()
  except UnicodeDecodeError:
      st.error("인코딩 오류가 발생했습니다. UTF-8로 재시도합니다.")
      df = pd.read_csv(filepath, encoding='utf-8-sig')
  ```

- [ ] **데이터 검증**
  - 필수 컬럼 존재 여부 확인
  - 데이터 타입 검증
  - 빈 DataFrame 처리

- [ ] **사용자 친화적 에러 메시지**
  - 기술적 에러 대신 명확한 안내 메시지
  - 해결 방법 제시

#### 3.1.3 성능 최적화 확인
- [ ] **캐싱 전략 검토**
  - `@st.cache_data` 적용 여부 확인
  - 캐시 만료 정책 설정 (필요 시)
  
- [ ] **데이터 로딩 최적화**
  - 불필요한 컬럼 제거
  - 메모리 효율적인 데이터 타입 사용

---

### 3.2 재현 가능한 실행 환경 구축 (1~2시간)

#### 3.2.1 의존성 관리
- [ ] **requirements.txt 검증 및 업데이트**
  - 현재 사용 중인 모든 패키지 포함 여부 확인
  - 버전 고정 (production 안정성)
  ```txt
  streamlit==1.39.0
  pandas==2.2.3
  altair==5.4.1
  numpy==1.26.4      # 필요 시 추가
  plotly==5.18.0     # 사용 시 추가
  ```

- [ ] **Python 버전 명시**
  - `.python-version` 파일 생성 또는
  - `runtime.txt` 생성 (Streamlit Cloud용)
  ```txt
  python-3.11
  ```

- [ ] **개발 의존성 분리 (선택)**
  - `requirements-dev.txt` 생성
  ```txt
  pytest==7.4.3
  black==23.12.0
  flake8==6.1.0
  ```

#### 3.2.2 설정 파일 정리
- [ ] **`.streamlit/config.toml` 생성**
  ```toml
  [theme]
  primaryColor = "#FF4B4B"
  backgroundColor = "#FFFFFF"
  secondaryBackgroundColor = "#F0F2F6"
  textColor = "#262730"
  font = "sans serif"
  
  [server]
  headless = true
  port = 8501
  enableCORS = false
  enableXsrfProtection = true
  
  [browser]
  gatherUsageStats = false
  ```

- [ ] **`.gitignore` 생성/업데이트**
  ```gitignore
  __pycache__/
  *.pyc
  *.pyo
  *.egg-info/
  .venv/
  venv/
  .DS_Store
  .streamlit/secrets.toml
  *.log
  ```

#### 3.2.3 실행 가이드 문서화
- [ ] **README.md 업데이트**
  - 프로젝트 설명
  - 설치 방법
  - 실행 방법
  - 사용 방법
  - 문제 해결 (Troubleshooting)

  ```markdown
  # 서울교통공사 지하철 혼잡도 대시보드
  
  ## 설치 및 실행
  
  ### 1. Python 환경 준비
  ```bash
  python --version  # Python 3.11+ 필요
  ```
  
  ### 2. 의존성 설치
  ```bash
  pip install -r requirements.txt
  ```
  
  ### 3. 실행
  ```bash
  streamlit run app.py
  ```
  
  브라우저에서 http://localhost:8501 접속
  
  ## 데이터
  - 파일: `서울교통공사_지하철혼잡도정보_20250930.csv`
  - 출처: 서울교통공사 공공데이터
  
  ## 주요 기능
  - 호선별/역별 혼잡도 조회
  - 시간대별 혼잡 패턴 시각화
  - Top N 혼잡 역 랭킹
  
  ## 문제 해결
  - 한글 깨짐: 파일 인코딩 확인 (cp949/euc-kr)
  - 실행 오류: Python 버전 및 의존성 재설치
  ```

- [ ] **간단한 실행 스크립트 작성 (선택)**
  - `run.sh` (Linux/Mac)
  ```bash
  #!/bin/bash
  streamlit run app.py --server.port 8501
  ```
  
  - `run.bat` (Windows)
  ```batch
  @echo off
  streamlit run app.py --server.port 8501
  pause
  ```

---

### 3.3 배포 옵션 정리 및 선택 (1~2시간)

#### 3.3.1 배포 방식 비교 분석

| 배포 방식 | 장점 | 단점 | 추천 상황 |
|-----------|------|------|-----------|
| **Streamlit Community Cloud** | • 무료<br>• 설정 간단<br>• GitHub 연동 자동 | • 공개 저장소만<br>• 리소스 제한<br>• 커스텀 도메인 제한 | 공개 프로젝트, 빠른 프로토타입 |
| **사내 서버** | • 데이터 보안<br>• 리소스 커스텀<br>• 내부 접근 제어 | • 인프라 필요<br>• 유지보수 필요 | 기업 내부용, 민감 데이터 |
| **Docker** | • 환경 독립성<br>• 확장성<br>• 어디서나 실행 | • Docker 학습 필요<br>• 초기 설정 복잡 | 멀티 환경, 프로덕션 |
| **클라우드 VM** | • 완전한 제어<br>• 스케일링 가능 | • 비용 발생<br>• 관리 필요 | 상용 서비스, 높은 트래픽 |

#### 3.3.2 옵션별 배포 가이드

##### 옵션 A: Streamlit Community Cloud (추천 - 가장 간단)

**준비 사항:**
- [ ] GitHub 계정
- [ ] 공개 저장소 (private은 유료)

**배포 단계:**
1. [ ] GitHub에 저장소 생성 및 코드 푸시
   ```bash
   git init
   git add .
   git commit -m "Initial commit for Phase 3"
   git branch -M main
   git remote add origin https://github.com/username/subway-congestion.git
   git push -u origin main
   ```

2. [ ] [share.streamlit.io](https://share.streamlit.io) 접속 및 로그인

3. [ ] "New app" 클릭 → 저장소/브랜치/파일 선택
   - Repository: `username/subway-congestion`
   - Branch: `main`
   - Main file path: `app.py`

4. [ ] 배포 완료 후 URL 확인
   - 형식: `https://username-subway-congestion-app-xxxxx.streamlit.app`

5. [ ] (선택) Secrets 설정
   - 설정 → Secrets → TOML 형식으로 비밀 정보 추가

**장점:** 5분 내 배포 가능, 무료

---

##### 옵션 B: 사내 서버 배포

**준비 사항:**
- [ ] Linux 서버 접근 권한
- [ ] Python 3.11+ 설치
- [ ] 방화벽 포트 8501 오픈

**배포 단계:**
1. [ ] 서버에 코드 복사
   ```bash
   scp -r . user@server:/opt/subway-dashboard/
   ```

2. [ ] 가상환경 생성 및 의존성 설치
   ```bash
   cd /opt/subway-dashboard
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

3. [ ] systemd 서비스 생성 (`/etc/systemd/system/streamlit-dashboard.service`)
   ```ini
   [Unit]
   Description=Streamlit Subway Dashboard
   After=network.target
   
   [Service]
   Type=simple
   User=streamlit-user
   WorkingDirectory=/opt/subway-dashboard
   Environment="PATH=/opt/subway-dashboard/venv/bin"
   ExecStart=/opt/subway-dashboard/venv/bin/streamlit run app.py --server.port 8501 --server.headless true
   Restart=always
   
   [Install]
   WantedBy=multi-user.target
   ```

4. [ ] 서비스 시작 및 활성화
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl start streamlit-dashboard
   sudo systemctl enable streamlit-dashboard
   sudo systemctl status streamlit-dashboard
   ```

5. [ ] (선택) Nginx 리버스 프록시 설정
   ```nginx
   server {
       listen 80;
       server_name dashboard.company.com;
       
       location / {
           proxy_pass http://localhost:8501;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection "upgrade";
           proxy_set_header Host $host;
       }
   }
   ```

**장점:** 내부 데이터 보안, 완전한 제어

---

##### 옵션 C: Docker 컨테이너

**준비 사항:**
- [ ] Docker 설치

**배포 단계:**
1. [ ] `Dockerfile` 작성
   ```dockerfile
   FROM python:3.11-slim
   
   WORKDIR /app
   
   # 의존성 설치
   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt
   
   # 애플리케이션 코드 복사
   COPY . .
   
   # Streamlit 포트 노출
   EXPOSE 8501
   
   # 헬스체크
   HEALTHCHECK CMD curl --fail http://localhost:8501/_stcore/health || exit 1
   
   # 실행 명령
   ENTRYPOINT ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
   ```

2. [ ] `.dockerignore` 작성
   ```
   __pycache__
   *.pyc
   .git
   .venv
   venv
   *.md
   ```

3. [ ] 이미지 빌드
   ```bash
   docker build -t subway-dashboard:latest .
   ```

4. [ ] 컨테이너 실행
   ```bash
   docker run -d \
     --name subway-dashboard \
     -p 8501:8501 \
     -v $(pwd)/서울교통공사_지하철혼잡도정보_20250930.csv:/app/서울교통공사_지하철혼잡도정보_20250930.csv \
     subway-dashboard:latest
   ```

5. [ ] (선택) Docker Compose 사용
   ```yaml
   # docker-compose.yml
   version: '3.8'
   
   services:
     dashboard:
       build: .
       ports:
         - "8501:8501"
       volumes:
         - ./서울교통공사_지하철혼잡도정보_20250930.csv:/app/서울교통공사_지하철혼잡도정보_20250930.csv
       restart: unless-stopped
   ```
   
   실행:
   ```bash
   docker-compose up -d
   ```

**장점:** 환경 독립성, 어디서나 동일하게 실행

---

### 3.4 테스트 및 검증 (30분~1시간)

#### 3.4.1 기능 테스트 체크리스트
- [ ] **데이터 로딩 테스트**
  - CSV 파일이 정상적으로 로드되는가?
  - 한글이 깨지지 않는가?
  - 캐싱이 작동하는가?

- [ ] **필터링 테스트**
  - 각 필터 조합이 정상 작동하는가?
  - 빈 결과 시 적절한 메시지가 표시되는가?
  - 필터 초기화가 작동하는가?

- [ ] **시각화 테스트**
  - 라인차트가 정상 렌더링되는가?
  - 히트맵이 정상 렌더링되는가?
  - 인터랙티브 기능이 작동하는가?

- [ ] **랭킹 테스트**
  - Top N 정렬이 올바른가?
  - 표시 형식이 적절한가?

- [ ] **다운로드 테스트**
  - CSV 다운로드가 작동하는가?
  - 파일 내용이 올바른가?

#### 3.4.2 성능 테스트
- [ ] 초기 로딩 시간 측정 (목표: < 3초)
- [ ] 필터 변경 시 응답 시간 (목표: < 1초)
- [ ] 메모리 사용량 확인

#### 3.4.3 브라우저 호환성 테스트
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

#### 3.4.4 반응형 테스트
- [ ] 데스크톱 (1920x1080)
- [ ] 태블릿 (768x1024)
- [ ] 모바일 (375x667)

---

### 3.5 문서화 및 인수인계 (30분)

#### 3.5.1 운영 매뉴얼 작성
- [ ] **사용자 가이드**
  - 화면별 기능 설명
  - 스크린샷 포함
  - FAQ

- [ ] **개발자 가이드**
  - 코드 구조 설명
  - 주요 함수 설명
  - 확장 가이드

- [ ] **배포 가이드**
  - 배포 방법별 단계
  - 트러블슈팅
  - 롤백 방법

#### 3.5.2 변경 이력 관리
- [ ] `CHANGELOG.md` 작성
  ```markdown
  # Changelog
  
  ## [1.0.0] - 2024-12-16
  
  ### Added
  - 호선별 혼잡도 조회 기능
  - 시간대별 라인차트
  - Top N 랭킹
  - 히트맵 시각화
  - CSV 다운로드
  
  ### Fixed
  - 한글 인코딩 문제 해결
  - 필터링 버그 수정
  ```

---

## 📊 Phase 3 완료 기준 (Definition of Done)

### 필수 (Must Have)
- [ ] 코드가 3개 모듈로 명확히 분리됨 (data.py, charts.py, app.py)
- [ ] 모든 함수에 docstring과 타입 힌트 추가
- [ ] requirements.txt가 최신 상태로 업데이트됨
- [ ] README.md에 설치/실행 가이드 작성됨
- [ ] 배포 방식 1개 이상 문서화 및 테스트 완료
- [ ] 기능 테스트 체크리스트 100% 통과

### 권장 (Should Have)
- [ ] .streamlit/config.toml 설정 완료
- [ ] .gitignore 작성
- [ ] Docker 또는 Streamlit Cloud 배포 완료
- [ ] 에러 핸들링 강화
- [ ] 성능 최적화 (캐싱, 로딩 시간)

### 선택 (Nice to Have)
- [ ] 개발 의존성 분리 (requirements-dev.txt)
- [ ] 자동화된 테스트 코드 작성
- [ ] CI/CD 파이프라인 구축
- [ ] 로깅 시스템 추가
- [ ] 사용자 분석 (Google Analytics 등)

---

## 🚀 실행 우선순위

### 우선순위 1 (즉시 실행)
1. 코드 리뷰 및 docstring 추가
2. requirements.txt 검증
3. README.md 작성
4. 기능 테스트

### 우선순위 2 (당일 완료)
5. 배포 방식 선택 및 문서화
6. .gitignore 및 config.toml 작성
7. 에러 핸들링 강화

### 우선순위 3 (시간 여유 시)
8. Docker 설정
9. 운영 매뉴얼 작성
10. CHANGELOG.md 작성

---

## 💡 추천 배포 방식

현재 프로젝트 특성상 **Streamlit Community Cloud** 추천:
- ✅ 무료이고 설정이 가장 간단 (5분 내 배포)
- ✅ GitHub 연동 자동화
- ✅ HTTPS 자동 제공
- ✅ 코드 업데이트 시 자동 재배포

만약 **내부 데이터 보안**이 중요하면 → **사내 서버** 배포
만약 **멀티 환경 지원** 필요하면 → **Docker** 배포

---

## 📝 체크리스트 요약

```
Phase 3 작업 항목 (총 40개)

□ 3.1 코드 구조화 (15개)
  □ 모듈 역할 명확화
  □ docstring 작성
  □ 타입 힌트 추가
  □ 매직 넘버 제거
  □ 에러 핸들링 강화
  □ 성능 최적화 확인
  (... 생략)

□ 3.2 재현 가능한 실행 환경 (8개)
  □ requirements.txt 검증
  □ Python 버전 명시
  □ config.toml 생성
  □ .gitignore 생성
  □ README.md 업데이트
  (... 생략)

□ 3.3 배포 옵션 정리 (3개)
  □ 배포 방식 비교
  □ 선택한 방식 가이드 작성
  □ 배포 테스트

□ 3.4 테스트 및 검증 (10개)
  □ 기능 테스트
  □ 성능 테스트
  □ 브라우저 호환성 테스트
  (... 생략)

□ 3.5 문서화 (4개)
  □ 사용자 가이드
  □ 개발자 가이드
  □ CHANGELOG.md
  (... 생략)
```

---

## 🎯 다음 단계

Phase 3 완료 후:
1. **모니터링 설정**: 사용자 트래픽, 에러 추적
2. **피드백 수집**: 실제 사용자로부터 개선 사항 파악
3. **Phase 4 계획**: 추가 기능 (알림, 예측, API 등)

---

**작성일**: 2024-12-16  
**작성자**: AI Assistant  
**버전**: 1.0
